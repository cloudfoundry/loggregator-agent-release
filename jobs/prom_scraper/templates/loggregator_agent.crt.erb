<%=
  cert = nil
  if_p('loggregator_agent.tls.cert') { |agent_cert|
    cert = agent_cert
  }.else_if_link("loggregator") { |loggregator|
    cert = loggregator.p("loggregator.tls.doppler.cert")
  }
  cert
%>
