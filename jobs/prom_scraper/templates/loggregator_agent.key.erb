<%=
  key = nil
  if_p('loggregator_agent.tls.key') { |agent_key|
    key = agent_key
  }.else_if_link("loggregator") { |loggregator|
    key = loggregator.p("loggregator.tls.doppler.key")
  }
  key
%>
